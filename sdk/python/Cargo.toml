[package]
name = "a3s-code-py"
version = "9.0.0"
edition = "2021"
authors = ["A3S Lab Team"]
license = "MIT"
repository = "https://github.com/A3S-Lab/Code"
description = "A3S Code Python bindings - Native Python module via PyO3"

[lib]
name = "a3s_code"
crate-type = ["cdylib"]

[dependencies]
a3s-code-core = { version = "9.0.0", path = "../../core", default-features = false }
pyo3 = { version = "0.23", features = ["multiple-pymethods"] }
tokio = { version = "1.35", features = ["full"] }
tokio-util = "0.7"
serde_json = "1.0"
serde = "1.0"
anyhow = "1.0"
async-trait = "0.1"
num_cpus = "1.16"

[features]
# Thin coding-harness default (`HARNESS-CONV5`). Enable `advanced-harness`,
# `server`, and/or `headless-search` explicitly for product surfaces.
default = ["a3s-vec-fts"]
# Coding-only: `--no-default-features --features a3s-vec-fts`
headless-search = ["a3s-code-core/headless-search"]
s3 = ["a3s-code-core/s3"]
server = ["s3"]
advanced-harness = ["a3s-code-core/advanced-harness"]
extension-module = ["pyo3/extension-module", "pyo3/abi3-py310"]
a3s-vec-fts = ["a3s-code-core/a3s-vec-fts"]
