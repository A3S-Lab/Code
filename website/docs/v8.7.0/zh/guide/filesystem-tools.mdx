---
title: 'AgentDir tools（已移除）'
description: 'AgentDir tools/ 与 script 规格已随 serve 移除'
---

# AgentDir tools（已移除）

AgentDir `tools/`（`kind: mcp` / `kind: script`）与 `AgentDirScriptTool`
属于文件系统优先 serve 面，**已移除**。

请通过宿主直接工具 / SDK session 注册、MCP，或 Core 内置工具注册能力。
